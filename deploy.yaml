import:
  - recipe/laravel.php
  - recipe/deploy/writable.php

config:
  repository: 'https://github.com/hoangthanhlam1984/webservice-demo.git'

hosts:
  68.183.232.113:
    remote_user: root
    deploy_path: '~/webservice-demo'
    http_user: www-data
    http_group: www-data

tasks:
  build:
    - run: uptime

after:
  deploy:failed: deploy:unlock
