import:
  - recipe/laravel.php

config:
  repository: "https://github.com/hoangthanhlam1984/webservice-demo.git"
  remote_user: root

hosts:
  68.183.232.113:
    deploy_path: "~/projects/ws"

tasks:
  build:
    - cd: "{{release_path}}"
    - run: "composer install"

after:
  deploy:failed: deploy:unlock
